<h1 class="title is-1">List Of Restaurants</h1>

<div class="is-flex is-justify-content-flex-end">
    <%= paginate @restaurants %>
</div>

<div class="container">
<table class="table">
    <thead>
        <tr>
            <th>Restaurant Name</th>
            <%= res.Cuisine %>
            </td>
            <td>
                <%= link_to res.chef.ChefName, chef_path(res.chef) %>
            </td>
            <td>
                <%= link_to 'View ->', restaurant_path(res)%>
            </td>
        </tr>
        <% end %>
    </tbody>
</table>
</div>